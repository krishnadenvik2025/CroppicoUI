import { DisableOneSecDirective } from './disable-one-sec.directive';

describe('DisableOneSecDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableOneSecDirective();
    expect(directive).toBeTruthy();
  });
});
